# Production build: leave the backend URL empty so the app calls its own origin.
# FastAPI serves this bundle and the API from the same port, so no CORS and no
# proxy are involved. This file also prevents a local .env (which points at
# http://127.0.0.1:8000) from being baked into the production bundle.
VITE_BACKEND_URL=
