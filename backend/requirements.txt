# Backend dependencies
#
# Runs on Python 3.8+ (the same interpreter as the classifier, so
# /predict/current can load the pickled models in-process). The Dockerfile
# uses 3.11. Keep new code 3.8-compatible: use `typing.Dict` / `Optional`
# in pydantic models, since pydantic evaluates annotations at runtime.

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# The trained classifier runs in-process (app/ml_detector.py), so the backend
# needs the ML stack the models were pickled with. Versions match the training
# environment; a mismatch can make pickles unloadable.
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<1.6
xgboost>=2.0,<3.0

# Test suite (backend/tests)
pytest>=8.0.0
httpx>=0.26.0

# PostgreSQL persistence (Neon serverless Postgres)
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9
